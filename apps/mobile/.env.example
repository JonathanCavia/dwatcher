# Base URL for the dwatcher backend API
# When using an Android emulator, use: http://10.0.2.2:3000
# When using a physical device on the same network, use: http://<your-ip>:3000
# The signaling server for WebRTC is derived from this URL by the backend config
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
