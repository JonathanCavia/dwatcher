import { create } from 'zustand';

interface SessionStore {
  // Placeholder — session state will be added when auth is implemented
}

export const useSessionStore = create<SessionStore>(() => ({}));
