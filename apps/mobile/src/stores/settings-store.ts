import { create } from 'zustand';

interface SettingsStore {
  // Placeholder — settings state will be added when configuration UI is implemented
}

export const useSettingsStore = create<SettingsStore>(() => ({}));
