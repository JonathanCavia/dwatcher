## Web Application — Not Applicable

This project does not have a web application in its initial scope.

The `apps/backend` serves the signaling server and REST API, but there is no
web frontend in this monorepo. A viewer dashboard (web) is planned for
Phase 4-7 of the roadmap but does not exist yet.

When the web viewer is added, this file should be updated with web app conventions.
